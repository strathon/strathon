# Tools the CI workflow installs directly, pinned so a maintainer's
# release cannot change what a job checks. They live in a requirements
# file rather than inline in ci.yml because dependabot's github-actions
# ecosystem only tracks `uses:` versions -- a pin inside a `run:` step is
# invisible to it and would rot with nothing to say so. The pip entry for
# "/" in .github/dependabot.yml watches this file.
bandit==1.9.4
ruff==0.16.1
